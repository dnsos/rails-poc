<!DOCTYPE html>
<html>
  <head>
    <title>RailsPoc</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <link href="https://unpkg.com/maplibre-gl@1.15.2/dist/maplibre-gl.css" rel="stylesheet" />

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="h-screen overflow-hidden">
    <header class="h-[49px] py-2 px-3 flex justify-between gap-2 items-center border-b border-b-neutral-200">
      <%= link_to "Home", root_path, class: 'py-1 font-medium text-lime-600' %>
      <div>
        <%= yield :top_right %>
      </div>
    </header>
    <main class="h-[calc(100vh-49px)]">
      <%= yield %>
    </main>
  </body>
</html>
